PKG="{{ package }}"
VERSION="{{ version }},REV={{ revision }}"
NAME="{{ summary }}"
DESC="{{ description }}"
ARCH="{{ arch }}"
SUNW_ISA="{{ isa }}"
CLASSES="none"
VENDOR="{{ company }}"
BASEDIR="/"
CATEGORY="application"
